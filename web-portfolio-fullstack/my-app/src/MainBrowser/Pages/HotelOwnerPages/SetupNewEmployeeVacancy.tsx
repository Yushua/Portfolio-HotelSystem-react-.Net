import { Box, Grid } from '@mui/material';
import './Dashboard.css';
import React from 'react';
import RoleSetup from '../../../Login/RoleSetup';

function SetupNewEmployeeVacancy() {
  return (
    <>
      button to setup new vacanices.

      description of what the job entails

      each vacancie only has the data from the user and the description THEY put forward.
    </>
  );
}

export default SetupNewEmployeeVacancy;